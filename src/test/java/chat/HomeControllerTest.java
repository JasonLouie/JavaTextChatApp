package chat;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

import com.chat.controller.HomeController;

import java.io.IOException;

@SuppressWarnings("unused")
@ExtendWith(MockitoExtension.class)
public class HomeControllerTest {
    /*
    @Test
    public void testDisplayCurrentUserProfile() throws IOException {
        HomeController homeController = new HomeController();
        homeController.displayCurrentUserProfile();
        // Verify that the user profile is displayed correctly
    }

    @Test
    public void testDisplayError() throws IOException {
        HomeController homeController = new HomeController();
        homeController.displayError("Error message");
        // Verify that the error message is displayed correctly
    }

    @Test
    public void testHandleLogout() throws IOException {
        HomeController homeController = new HomeController();
        homeController.handleLogout();
        // Verify that the user is logged out correctly
    }

    @Test
    public void testHandleSearch() throws IOException {
        HomeController homeController = new HomeController();
        homeController.handleSearch();
        // Verify that the search results are displayed correctly
    }

    @Test
    public void testHandleFriends() throws IOException {
        HomeController homeController = new HomeController();
        homeController.handleFriends();
        // Verify that the friends list is displayed correctly
    }

    @Test
    public void testHandleMessages() throws IOException {
        HomeController homeController = new HomeController();
        homeController.handleMessages();
        // Verify that the messages list is displayed correctly
    }
    */
}
